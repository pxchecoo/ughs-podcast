{
  "name": "university-en-vivo",
  "version": "1.0.0",
  "private": true,
  "description": "University En Vivo website and Vercel analytics API",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "check": "node --check api/*.js && node --check admin/admin.js && node -e \"JSON.parse(require('node:fs').readFileSync('vercel.json', 'utf8'))\"",
    "test": "node --test"
  },
  "dependencies": {
    "@google-analytics/data": "^6.1.0",
    "@upstash/ratelimit": "^2.1.0",
    "@upstash/redis": "^1.38.4"
  },
  "type": "module"
}
