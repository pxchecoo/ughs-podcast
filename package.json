{
  "name": "university-en-vivo",
  "version": "1.0.0",
  "private": true,
  "description": "University En Vivo website and Vercel analytics API",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "check": "node --check api/analytics.js && node --check api/_analytics.js && node --check api/_auth.js",
    "test": "node --test"
  },
  "dependencies": {
    "@google-analytics/data": "^6.1.0"
  },
  "type": "module"
}
